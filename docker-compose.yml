version: '3.8'

services:
  redis:
    image: "redis:8.0.3-bookworm"
    container_name: cleanforum_redis
    command: redis-server --appendonly yes
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

volumes:
  redis_data:
