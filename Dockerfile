# Шаг 1: Выбираем базовый образ с нужной версией Python
# Используем 'slim-bookworm' как легковесный и современный вариант
FROM python:3.11-slim-bookworm

# Шаг 2: Устанавливаем рабочую директорию внутри контейнера
WORKDIR /app

# Шаг 3: Копируем файл с зависимостями и устанавливаем их
# Копируем только requirements.txt сначала, чтобы Docker мог кэшировать этот слой.
# Это ускоряет сборку, если код меняется, а зависимости - нет.
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Шаг 4: Копируем все файлы проекта в рабочую директорию
COPY . .

# Шаг 5: Сообщаем Docker, что приложение будет слушать порт 8000
EXPOSE 8000

# Шаг 6: Указываем команду для запуска приложения
# Используем uvicorn для запуска ASGI-приложения (main:app)
# --host 0.0.0.0 делает сервер доступным извне контейнера
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
